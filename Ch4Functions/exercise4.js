function scream(words) {
    words = words + '!!!!';
    return;
    console.log(words);
  }
  
  scream('Yipeee');

//   What does the following code log to the console?

// It will not log anything because the function is stopped with the return word before console.log is called.
